name: Cleanup EC2 Disk

on:
  workflow_dispatch:  # Manual trigger only (requires secrets setup)
  # Uncomment below to enable daily automatic cleanup:
  # schedule:
  #   - cron: '0 3 * * *'  # Daily at 3 AM UTC

jobs:
  cleanup:
    name: Clean EC2 Disk Space
    runs-on: ubuntu-latest
    
    steps:
      - name: Check secrets
        run: |
          echo "Checking secrets configuration..."
          
          if [ -z "${{ secrets.EC2_SSH_KEY }}" ]; then
            echo "❌ ERROR: EC2_SSH_KEY secret is not set!"
            exit 1
          else
            echo "✅ EC2_SSH_KEY is set"
          fi
          
          if [ -z "${{ secrets.EC2_HOST }}" ]; then
            echo "❌ ERROR: EC2_HOST secret is not set!"
            exit 1
          else
            echo "✅ EC2_HOST is set: ${{ secrets.EC2_HOST }}"
          fi
          
          echo ""
          echo "All secrets configured correctly!"

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.EC2_SSH_KEY }}" > ~/.ssh/ec2-key.pem
          chmod 600 ~/.ssh/ec2-key.pem
          
          # Disable strict host key checking for GitHub Actions
          cat >> ~/.ssh/config <<EOF
          Host ${{ secrets.EC2_HOST }}
            StrictHostKeyChecking no
            UserKnownHostsFile=/dev/null
          EOF
          
          echo "SSH setup complete for ${{ secrets.EC2_HOST }}"

      - name: Run cleanup script
        run: |
          ssh -i ~/.ssh/ec2-key.pem ubuntu@${{ secrets.EC2_HOST }} << 'EOF'
            cd /home/ubuntu/goondvr
            
            echo "🧹 Running disk cleanup..."
            ./scripts/cleanup-disk.sh
            
            echo ""
            echo "📊 Final Status:"
            df -h /
            sudo docker system df
          EOF

      - name: Check disk usage
        run: |
          ssh -i ~/.ssh/ec2-key.pem ubuntu@${{ secrets.EC2_HOST }} << 'EOF'
            DISK_USAGE=$(df / | tail -1 | awk '{print $5}' | sed 's/%//')
            
            if [ $DISK_USAGE -gt 85 ]; then
              echo "⚠️ WARNING: Disk usage is ${DISK_USAGE}% (> 85%)"
              exit 1
            elif [ $DISK_USAGE -gt 75 ]; then
              echo "⚠️ CAUTION: Disk usage is ${DISK_USAGE}% (> 75%)"
            else
              echo "✅ Disk usage is healthy: ${DISK_USAGE}%"
            fi
          EOF

      - name: Cleanup SSH
        if: always()
        run: rm -f ~/.ssh/ec2-key.pem
